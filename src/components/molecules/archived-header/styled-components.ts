import styled from "@emotion/styled";

export const NotesStatistic = styled.div`
    margin: 30px;

    .notes-statistic-icon {
        width: 50px;
        color: white;
        background: #616161;
        height: 70%;
        display: flex;
        justify-content: center;
        align-items: center;
        border-radius: 40px;
    };

    .notes-statistic-header {
        display: flex;
        background-color: #999999;
        color: white;
        padding: 10px 20px;
        border-radius: 5px;
    };

    .notes-statistic-category {
        margin-left: 50px;
    };

    .notes-statistic-header {
        font-size: 20px;
    };

    .notes-statistic-category {
        width: 50%;
    };

    .notes-statistic-active {
        width: 25%;
    };

    .archived {
        width: 35%;
    };

    .notes-statistic-card {
        background-color: #e8eaf5;
        display: flex;
        align-items: center;
        font-size: 20px;
        padding: 0px 20px;
        height: 60px;
        margin: 10px 0;
        border-radius: 5px;
    };

    .notes-statistic-icon {
        width: 50px;
    };

    .notes-statistic-name {
        width: 50%;
    };

    .notes-statistic-active {
        width: 25%;
    };

    .notes-statistic-archived {
        width: 35%;
    };
`;
